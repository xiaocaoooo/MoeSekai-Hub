name: Story Asset Full Update

on:
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:
    inputs:
      lang_srcs:
        description: "Per-language source lists, semicolon-separated. e.g. jp haruki sekai.best;cn haruki sekai.best"
        required: false
        default: "jp haruki sekai.best;cn haruki sekai.best"

permissions:
  contents: write

jobs:
  full-update:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Setup uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --locked

      - name: Full update story assets
        id: asset
        continue-on-error: true
        run: |
          LANG_SRCS="${{ inputs.lang_srcs || 'jp haruki sekai.best;cn haruki sekai.best' }}"
          ARGS=""
          IFS=';' read -ra ENTRIES <<< "$LANG_SRCS"
          for entry in "${ENTRIES[@]}"; do
            ARGS="$ARGS --lang-srcs $entry"
          done
          uv run python -m src.cli update-story-asset --full $ARGS

      - name: Generate latest story summary
        id: summary
        continue-on-error: true
        env:
          STORY_SUMMARY_API_KEY: ${{ secrets.STORY_SUMMARY_API_KEY }}
          STORY_SUMMARY_BASE_URL: ${{ vars.STORY_SUMMARY_BASE_URL }}
          STORY_SUMMARY_MODEL: ${{ vars.STORY_SUMMARY_MODEL }}
        run: uv run python -m src.cli update-story-summary

      - name: Fail if all tasks failed
        if: ${{ steps.asset.outcome == 'failure' && steps.summary.outcome == 'failure' }}
        run: |
          echo "All story update tasks failed."
          exit 1

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          if [ -d story_assets ]; then
            git add story_assets/
          fi
          if [ -d story ]; then
            git add story/
          fi

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "chore(data): full story asset and summary update [skip ci]"
          git push
